# PYTHON TEST

## 1. What is the difference between mutable and immutable data types in Python?

## 2. What is indentation in Python and why is it important?

## 3. Write a program to check whether a number is even or odd.

## 4. Explain the difference between `append()` and `extend()` in lists.

## 5. Write a program to find the smallest number in a list.

## 6. Create a tuple with 5 elements and print its last element.

## 7. What is dictionary nesting? Give a simple example.

## 8. Create a NumPy 2D array and print all diagonal elements.

## 9. Write a program to remove spaces from a string.

## 10. Check whether a given year is a leap year or not.

## 11. Print all numbers between 1 to 100 that are divisible by both 3 and 5 without using `AND` and by using only 1 condition.

## 12. Write a program to calculate the sum of even numbers from 1 to n.

## 13. Print the Fibonacci series up to n terms.

## 14. A shop records 10 customer bills

- If `bill amount < 0` → ignore it  
- If `bill amount > 5000` → count as premium customer  
- Otherwise store normal bill amounts in a list  
- At the end print premium customer count and normal bills list

## 15. Write a function to check whether a string starts with a vowel or not.

## 16. Write a function to find the maximum element in a tuple.

## 17. What is the difference between `break` and `continue` statements?

## 18. How to drop missing values in Pandas? Give code examples.

## 19. Write code to rename columns in a Pandas DataFrame.

## 20. Create a line chart using any sample data.

## 21. Create a histogram using random values.

## 22. Given a dataset of employee salaries

- Replace salaries below `15000` with `15000`
- Find average salary
- Display highest 2 salaries

## 23. Explain the difference between `shape` and `size` in NumPy with examples.

## 24. Create a NumPy array of numbers from 1 to 20 and perform filtering on them for values `> 10`.

## 25. Write code to merge two DataFrames in Pandas using a common column.
## 24. Create a NumPy array of numbers from 1 to 20 and perform filtering on them for values `> 10`.

## 25. Write code to merge two DataFrames in Pandas using a common column.
